import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    containerPadding: {
        padding: 10
    },
    centralize: {
        justifyContent: 'center',
        alignItems: 'center'
    },
    input: {
        padding: 10
    }
});

export const colors = {
    blue: '#73a9e5'
}